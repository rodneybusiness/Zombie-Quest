# Security

- **No secrets** - Single-player game, no API keys needed
- **Asset paths** - Use relative paths; don't hardcode absolute paths
- **JSON parsing** - `game_data.json` is trusted; no need for sanitization
